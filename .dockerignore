# The context is a finished tree, not a source checkout: node_modules, apps/daemon/dist and
# apps/web/dist are produced on the runner and copied in verbatim. Nothing else is read.
*
!node_modules
!apps/daemon/dist
!apps/web/dist
