{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Consumed from source by both apps; nothing is emitted from here.
    "noEmit": true,
    "allowImportingTsExtensions": true
  },
  "include": ["src/**/*.ts"]
}
